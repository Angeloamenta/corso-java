// metodi stringhe
// equals, equalsIgnoreCase, compareTo, compareToIgnoreCase, length, charAt, toUpperCase, toLowerCase, trim, substring, indexOf, lastIndexOf, startsWith, endsWith, replace, replaceAll, split, concat, valueOf, toString, format, intern

public class stringhe {
    public static void main(String[] args) {
        // System.out.println("Stringhe");
        String nome = "     Leonardo    ";
        // System.out.println(nome);


        // equals case sensitive
        // Boolean risultato = nome.equals("Leonardo");

        // length
        // int risultato = nome.length();

        // charat ( mi dice la posizione del carattere)
        // char risultato = nome.charAt(0);

        // indexOf restituisce l'indice del carattere
        //  int risultato = nome.indexOf('o');

        // // isempty restituisce true se la stringa è vuota
        // Boolean risultato = nome.isEmpty();

        // toUpperCase trasforma la stringa in maiuscolo
        //  String risultato = nome.toUpperCase();

        // toLowerCase trasforma la stringa in minuscolo
        // String risultato = nome.toLowerCase();

        // trim elimina gli spazi bianchi
        // String risultato = nome.trim();

        // replace sostituisce un carattere con un altro
        // String risultato = nome.replace('o', 'a');


        // System.out.println(risultato);
    }
}
